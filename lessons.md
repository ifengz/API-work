# Lessons

1. 用户一旦收口范围，先改计划文档，再动实现。
2. AI Gateway 只管模型网关，不把业务系统接入混进来。
3. 多代理并行时，每个 worker 只碰自己的文件；监工和 reviewer 默认只读。
4. 当前目录不是 git 仓库，进度要落在文档里，不能假装有 git 流程。
5. 用户说“放第二盘”时，先确认是项目目录/数据边界，不默认上升为 Docker 或系统级迁移。
