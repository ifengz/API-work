# Task Issue

| 日期 | 任务 | 状态 | 备注 |
| --- | --- | --- | --- |
| 2026-04-05 | 为 `site-gateway` 设计并编排“生图/分析请求审计记录与分钟级汇总”最小改动方案 | 已实现，待验收 | 代码与测试已落地；等待你确认“谁”是否还要细到最终用户后，再决定是否补新请求头契约 |
| 2026-04-05 | 把 LiteLLM 默认 worker 从 `1` 提到 `4`，让现有多 deployment 能真正并发处理 | 已实现，待验收 | 只改运行并发开关，不碰模型路由、不碰审计逻辑；本地 `30 tests OK`，结构自检与 diff 检查已通过 |
| 2026-04-05 | 重启远端 `litellm` 让 `4` 个 worker 生效 | 阻塞 | 本机会话没有 `docker`；SSH 直连 `root@38.246.250.228` 卡在密码认证；当前浏览器执行器 `browser-use/browserbase/chrome-devtools/playwright` 全部 `Transport closed` |
| 2026-04-05 | 为 `site-gateway` 增加 chat/image 有序模型队列、顺序回退与尝试日志 | 已实现，待验收 | 用户指定 chat 5 个模型、生图 3 个模型按顺序尝试；显式模型优先、回退链入审计并打印日志；本地 `36 tests OK`、结构自检和 diff 检查已通过 |
| 2026-04-05 | 核实 Vertex Gemini 生图是否支持参考图随 API 传入，以及当前 `site-gateway` 链路是否真的带过去 | 进行中 | 先核官方合同，再核本仓库 `/v1/images/generations` 转发真值；不做猜测，不先改代码 |
| 2026-04-05 | 为生图链路补“参考图原样上传”能力，并保持原 prompt 与参考图内容不被改写 | 进行中 | 先补失败测试，再只改最小转发层；若还要改默认模型顺序，等用户给出精确顺序后再单独落地 |
| 2026-04-05 | 修复 `api-work` Gemini 多模态 chat 带图分析上游兼容问题，并补输入图日志 | 已实现，待验收 | 已按官方兼容口约束清洗 `image_url.detail`，并补“输入图数量进日志”的合同测试；本地 `38 tests OK`、结构自检和 diff 检查已通过 |
| 2026-04-06 | 推送当前 `api-work` 修复并重启宝塔线上 `site-gateway` | 已实现，待验收 | 已用宝塔终端把服务器本地改动先备份到 `/root/api-work-backup-20260405-173949/`，再 `git stash push -u` 保存为 `stash@{0}: pre-deploy-backup-20260405-173949`；随后已 `git pull --ff-only origin main`、`docker compose up -d --build site-gateway`，本地 `curl -i http://127.0.0.1:8080/healthz` 返回 `HTTP/1.1 200 OK` 与 `{\"status\":\"ok\"}` |
| 2026-04-06 | 线上验证并打通 Gemini 带图分析链路 | 已实现，待验收 | 已先用公网固定入口 `http://api-work.usfan.net:8080/v1/chat/completions` 对 `site-demo-a` 发 `text + image_url + detail:high` 真请求，当前线上仍返回 `503 UPSTREAM_UNAVAILABLE`，坐实故障点在后端多模态 chat 链路；随后已补“Gemini 带图 chat 走显式 `multimodal_chat_upstream=one_api`”的配置与代码，并通过 `python3 -m unittest discover -s tests`（`40 tests OK`）、`python3 scripts/check_project.py`、`git diff --check`，待推送并上线复测。 |
| 2026-04-06 | 修复宝塔线上 `litellm` 运行异常并完成公网 chat 可用性验收 | 进行中 | 当前 `8080 /healthz` 已正常，但公网 `POST /v1/chat/completions` 仍返回 `503 UPSTREAM_UNAVAILABLE`；已定位 `site-gateway` 到 `litellm` 存在 `Connection refused`，继续只用宝塔终端排查 `4000` readiness 与运行时配置。 |
| 2026-04-07 | 继续只用宝塔终端打通线上 `api-work` chat 链路，并确认其他项目何时可切并发 | 进行中 | 先确认 `litellm` 容器实际加载的配置与坏项目残留，再用公网固定入口复测；不碰宝塔中的其他项目，不做降级绕过。 |
| 2026-04-08 | 盘点现有 Vertex JSON 哪些仍可用，并用可用凭证跑通 `api-work` 最短测试链路 | 进行中 | 先验活本地 `vertex/` 与 `credentials/*` 里的 service account，再只用可用凭证重建配置并做一次真实请求；验收后再把状态收口为完成 |
| 2026-04-08 | 从现有 `vertex` / `credentials` JSON 中筛出仍可用的凭证，重建本地 Vertex 池并跑通带图分析链路测试 | 阻塞 | 已对 `vertex/**/*.json` 做真实 Google OAuth + Vertex `generateContent` 验活：`newwoo1` / `nodal-rex-492217-r2` / `slhww-492207` 共 `20` 个账号全部 `invalid_grant: account not found`；仅 `ninth-generator-479604-s6` 的 `8` 个账号能换 token，但全部在 Vertex 调用时报 `403 PERMISSION_DENIED`，明细为项目未开 billing；当前仓库内没有任何一份 JSON 能真正跑通 Vertex 模型调用 |
| 2026-04-08 | 用用户提供的 `New API` key 做最小真测，确认 `api-work` 是否可先借外部 OpenAI 兼容上游验证链路 | 阻塞 | 已对 `https://www.newapi.pro`、`https://newapi.pro`、`https://api.newapi.pro` 做真实请求；前两者返回前台页面 `404`，不是 API host；`https://api.newapi.pro` 的 `/api/usage/token` 与 `/v1/models` 都返回 `401 无效的令牌`。当前结论只剩两种：要么这把 key 已失效，要么它不属于官方 `api.newapi.pro`，而是别的 New API 实例；继续测试需要准确 base URL |
| 2026-04-11 | 研判 `1xm.ai` / `packyapi.com` / `api2.echoflow.cn` 的 `sk-` key 能否与 `api-work` 单 key 生图站接入方案混用 | 已分析，待确认 | 已核对外部公开合同与本仓库 `site-gateway` 真值：外部 key 只是各自平台的 Bearer API key；`api-work` 前端输入框里的是本站 `site_token`，仅在 `config/gateway.json` 命中后才有效。若前端保持“单 key + 单出口 `api-work`”，外部 key 不会自动变成 `api-work` 可用 key，也不会被透传成上游凭证 |
| 2026-04-11 | 质疑“把外部中转站 key 接进 `api-work` 统一管理，再给生图项目只发 `api-work` key”这条需求是否稳妥 | 已分析，待确认 | 已从生图链路、并发、取消中断、扣费幂等、可观测性、供应商耦合与故障归因几方面收口：方向可以做，但前提不是“兼容 newapi 就够”，而是必须把外部 provider 抽成独立上游资源池，并加任务态/幂等/熔断/限流；否则只是把外部平台的不稳定统一收编进 `api-work` |
| 2026-04-11 | 研究“怎样同时满足 Vertex JSON 路线、外部 API key 路线、且不给生图主链引入额外中间商故障”的可行架构 | 已分析，待确认 | 已深搜官方资料与实测：结论是推荐“三轨制”而不是单一平台。外部 OpenAI/NewAPI 兼容 key 走浏览器直连模式，Vertex JSON 只走服务端 broker 模式，可选再加一个托管池模式；若要重做平台，可用 `New API` 做控制面，但 Vertex 鉴权与生图任务态必须独立实现，不能只靠裸 `newapi` 兼容层 |
| 2026-04-11 | 按“双入口”方案改造 `api-work`，让 `Api-work Key` 能挂 AI Studio key 或 Vertex JSON，并补一份其他生图项目可照抄的接入文档 | 已实现，待验收 | 已补站点级 `model_route_overrides`、`ai_studio` 示例 upstream、合同测试与接入文档 [doc/image_dual_entry_phase1.md](/Users/ifengz/CodingCase/API-work/doc/image_dual_entry_phase1.md)；随后已用用户本机终端完成 AI Studio 真测：第一把 key 返回 `403 PERMISSION_DENIED`，第二把 key 返回 `200`，确认 `ai_studio` 接法可用 |
| 2026-04-12 | 放开 `*.usfan.net` 作为 `Api-work Key` 的受控来源域，并同步线上网关到 `https://103.106.188.73.sslip.io` | 已实现，待验收 | 已把 `allowed_origins` 从严格精确匹配扩成“受控后缀匹配”，只接受 `scheme://*.domain` 这种配置；CORS 仍回显具体 `Origin`，不返回裸 `*`；随后同步线上站点配置并用用户给定的 `Api-work Key` 做联调验收 |
